/* archivo 'potencias.h' es  almacenado en el directorio 'misIncludes'  */

/* función que regresa el cubo de un numero. */
float cubo(float a);
 
/* función que regresa la cuarta de un numero. */
float cuarta(float a);

/* función que regresa la quintilla de un numero. */
float quintilla(float a);